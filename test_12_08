def sum(list):
    if list == []:
        return 0
    return list[0] + sum(list[1:])
print(sum([2, 3, 4]))
# it worked, I`m just looking through functionality of a github. 
#comment created by doppleganger
